import React, { Component } from "react"

class Binding extends Component {
    render(){
        return(
            <div>
                <button>Click</button>
            </div>
        )
    }
}

export default Binding